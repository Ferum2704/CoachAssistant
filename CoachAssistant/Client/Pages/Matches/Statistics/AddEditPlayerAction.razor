@* 
<MudDialog>
    <DialogContent>
        <MudSelect T="Guid?" Label="Select teams to add" MultiSelection="true" @bind-SelectedValues="selectedTeams" ToStringFunc="@((guid) => GetTeamNameByGuid(guid))">
            <MudSelectItem T="Guid?" Value="@((Guid?)null)" Disabled="true">Select a team</MudSelectItem>
            @foreach (var team in AvailableActions)
            {
                <MudSelectItem T="Guid?" Value="@team.Id">@team.Name</MudSelectItem>
            }
        </MudSelect>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="@Submit">Add</MudButton>
        <MudButton Color="Color.Secondary" OnClick="@Cancel">Cancel</MudButton>
    </DialogActions>
</MudDialog>

@code {
    private ActionTypeModel selectedAction { get; set; }

    [Parameter]
    public List<ActionTypeModel> AvailableActions { get; set; }

    [CascadingParameter]
    MudDialogInstance MudDialog { get; set; }

    private string GetTeamNameByGuid(Guid? guid)
    {
        return AvailableTeams.FirstOrDefault(t => t.Id == guid)?.Name ?? "Unknown";
    }

    private void Submit()
    {
        MudDialog.Close(selectedTeams);
    }

    private void Cancel()
    {
        MudDialog.Close();
    }
}
 *@